apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-shorter.fullname" . }}-test-connection"
  labels:
    {{- include "go-shorter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-shorter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
